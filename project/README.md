## Setup
1. Clone the repo
2. Install dependencies: `pip install -r requirements.txt`
3. In VSCode, create `.vscode/settings.json`:
   {
       "python.analysis.extraPaths": ["../project/src"]
   }
4. Set `KAGGLE = False` in the notebook if running locally